apiVersion: apps/v1
kind: Deployment
metadata:
  name: atlas-llm-assistant
  labels:
    app: atlas-llm-assistant
    dxp.io/managed: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: atlas-llm-assistant
  template:
    metadata:
      labels:
        app: atlas-llm-assistant
        backstage.io/kubernetes-id: atlas-llm-assistant
    spec:
      nodeSelector:
        role: workload
      tolerations:
      - key: role
        operator: Equal
        value: workload
        effect: NoSchedule
      containers:
      - name: atlas-llm-assistant
        image: harbor.harbor.svc.cluster.local/dxp/atlas-llm-assistant:9905225
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000
        env:
        - name: LITELLM_URL
          value: "http://litellm.llmops.svc.cluster.local:4000"
        - name: LITELLM_API_KEY
          # S61 (Jeudi) : valeur en clair -- pas de mecanisme d'injection de
          # secret par service au provisioning aujourd'hui. Connu, accepte
          # pour la demo (meme niveau de maturite que le reste du POC),
          # a corriger si un vrai mecanisme de secret par service est
          # introduit (cf backlog : cles LiteLLM scopees par projet).
          value: "dxp-LiteLLM2026"
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 200m
            memory: 256Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: atlas-llm-assistant
spec:
  selector:
    app: atlas-llm-assistant
  ports:
  - port: 80
    targetPort: 8000
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: atlas-llm-assistant
  labels:
    app: atlas-llm-assistant
    dxp.io/managed: "true"
spec:
  ingressClassName: nginx
  rules:
  - host: atlas-llm-assistant.68.221.5.247.nip.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: atlas-llm-assistant
            port:
              number: 80
